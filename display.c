
void display_time(int hours, int minutes, int seconds, pi_framebuffer_t *dev);
void display_colons(pi_framebuffer_t *dev);
void dosplay_hours(int hours, pi_framebuffer_t *dev);
void dosplay_minutes(int minutes, pi_framebuffer_t *dev);
void dosplay_seconds(int seconds, pi_framebuffer_t *dev);
